Baseline: **408 Kotlin warnings / 110 files** in `app/src/main/java`.

Measurement command used throughout (a plain build reports nothing when the task is up to date, and `--rerun-tasks` alone hit a build-cache packing failure):

```bash
./gradlew :app:compileEntourageDebugKotlin --rerun-tasks --no-build-cache --console=plain
```

Tier totals, which partition the 408 exactly: Tier 1 = 52, Tier 2 = 100, Tier 3 = 207, Tier 4 = 49.

## 1. Baseline

- [x] 1.1 Re-run the measurement command and confirm the count is 408; save the raw output as the reference to diff against. Verify: warning count printed equals 408.
- [x] 1.2 Create `tools/utils/CompatExtensions.kt` (empty, package `social.entourage.android.tools.utils`) as the single home for Tier 3 helpers. Verify: `:app:compileEntourageDebugKotlin` still succeeds.

## 2. Tier 1 — Genuine defects (52 warnings)

Behaviour changes here are intended. For each task, record in the commit message what behaviour changed and why the old behaviour was wrong. "No visible change" is *not* the acceptance criterion for this tier.

- [x] 2.1 Delete the 5 unreachable duplicate `when` branches in `api/model/Events.kt:190,192,194,195,196` (`"animaux"`, `"cuisine"`, `"jeux"`, `"nature"`, `"sport"` — each already handled verbatim at lines 180,182,184,185,186). Do **not** touch lines 189/193/197 (`"activités manuelles"`, `"art & culture"`, `"rencontres nomades"`) — distinct keys that legitimately map to the same string. Verify: 5 `Duplicate branch condition` warnings gone; `showTagTranslated` still returns the right string for both accented and unaccented tags.
- [x] 2.2 Resolve the 5 `Condition is always` warnings individually — `actions/create/CreateActionStepFourFragment.kt:77` ('false'), `api/model/BaseEntourage.kt:347` ('false'), `groups/details/members/MembersFragment.kt:176,205` ('true'), `home/pedago/PedagoDetailActivity.kt:65` ('true'). For each, state whether it was dead code (delete the branch) or a wrong/inverted check (fix the condition) — do not blanket-delete. Verify: 5 warnings gone, each with a stated classification.
- [x] 2.3 Remove the 2 `Unnecessary non-null assertion (!!)` at `groups/details/members/MembersFragment.kt:177,206`, together with 2.2's fix for lines 176/205 — these are one defect each (a null check plus `!!` on a non-nullable `Int`). Verify: 2 warnings gone; member list still filters/paginates correctly.
- [x] 2.4 Fix the 2 `Java type mismatch: inferred type is 'Date?', but 'Date' was expected` at `events/create/CreateEventStepTwoFragment.kt:142,143` by handling the null case explicitly rather than asserting. Verify: 2 warnings gone; creating an event with and without an end date both work.
- [x] 2.5 Migrate the Places SDK across 6 files (37 warnings): `Place.Field.NAME`→`DISPLAY_NAME`, `ADDRESS`→`FORMATTED_ADDRESS`, `LAT_LNG`→`LOCATION`, and accessors `name`/`address`/`latLng`→`displayName`/`formattedAddress`/`location`. Files: `events/EventFiltersActivity.kt` (230,242,243,249,254), `main_filter/MainFilterActivity.kt` (373,378,383), `onboarding/onboard/OnboardingZoneChoiceActivity.kt` (267,268,269,277,278), `profile/EditProfileActivity.kt` (396,401,403,405), `user/edit/place/UserActionPlaceFragment.kt` (108,109,115,199,227), `user/edit/place/UserEditActionZoneFragment.kt` (104). Verify: warnings gone; place autocomplete returns a usable name+address+coordinates in each of the 4 pickers.
- [x] 2.6 Handle the 2 `Only safe (?.) or non-null asserted (!!.) calls are allowed on a nullable receiver` exposed by 2.5 at `events/EventFiltersActivity.kt:243` and `user/edit/place/UserActionPlaceFragment.kt:109` — the new Places accessors are properly `@Nullable`. Decide the null branch per site; do not use `!!`. Verify: 2 warnings gone; selecting a place with no formatted address does not crash.
- [x] 2.7 Fix the `Unchecked cast of 'ListAdapter!' to 'ArrayAdapter<String>'` at `onboarding/onboard/OnboardingZoneChoiceActivity.kt:249`. Verify: 1 warning gone; zone suggestion list still populates.
- [x] 2.8 Run the measurement command and confirm the count dropped by 52 (408 → 356), then run `./gradlew :app:testEntourageDebugUnitTest`. Verify: count is 356 and unit tests pass.

## 3. Tier 2 — Mechanical cleanups (100 warnings)

Strictly behaviour-preserving: the compiler has proved each redundancy. Per design D6, delete the redundant operator only — no renaming, no extracting, no incidental tidying inside these files.

- [x] 3.1 Remove 25 unnecessary safe calls in `tools/view/WebViewFragment.kt` (lines 92,120,133,137,138,139,140,153,161,162,176,188,198,251,258,263,268,293,296 and the rest reported) — ViewBinding fields are non-null. Verify: 25 warnings gone; the in-app web view still opens, shows its progress bar and closes.
- [x] 3.2 Remove 19 unnecessary safe calls in `guide/GDSSearchFragment.kt` (63,64,66,67,71,72,77,89,92,99,102,107,110,111,116,118,120,133,139). Verify: 19 warnings gone; guide search still filters and clears.
- [x] 3.3 Remove 8 unnecessary safe calls in `guide/GuideHubFragment.kt` (34,35,39,47,51,55,59). Verify: 8 warnings gone; guide hub still navigates.
- [x] 3.4 Remove 5 unnecessary safe calls in `tools/view/ShareMessageFragment.kt` (48,54,114,115,116). Verify: 5 warnings gone; share sheet still lists recipients and sends.
- [x] 3.5 Remove the remaining 10 unnecessary safe calls across `discussions/DetailConversationActivity.kt` (290,440,543,545,603×2,614,699,701), `api/model/Action.kt:123`, `home/HomeFragment.kt:1307`, `notifications/PushNotificationManager.kt:443`, `onboarding/login/LoginActivity.kt:119`, `report/ActionSheetFragment.kt:167`. Verify: all 67 `Unnecessary safe call` warnings are gone in total.
- [x] 3.6 Remove 17 needless casts (`No cast needed`) across `onboarding/onboard/OnboardingPhase1Fragment.kt` (360,377,395,434,461,462), `main_filter/MainFilterActivity.kt` (322,365,380), `notifications/NotificationActionManager.kt` (36,47,140), `badges/BadgeDetailBottomSheet.kt` (136,137), `badges/BadgesListAdapter.kt:77`, `onboarding/onboard/OnboardingZoneChoiceActivity.kt` (190,245). Verify: 17 warnings gone.
- [x] 3.7 Remove 8 always-left Elvis operators — `comment/PostAdapter.kt:833`, `enhanced_onboarding/fragments/OnboardingActionWishesFragment.kt:106,107`, `main_filter/MainFilterActivity.kt:412`, `members/MembersActivity.kt:171`, `profile/activities_settings/EditPasswordActivity.kt:39`, `profile/editProfile/EditPasswordFragment.kt:54`, `small_talks/SmallTalkActivity.kt:161`. Treat as dead-null-handling removal, but confirm per site that the left operand really is non-nullable rather than the type being wrong. Verify: 8 warnings gone.
- [x] 3.8 Remove 3 redundant conversion calls (`deeplinks/UniversalLinkManager.kt:390`, `report/ReportModalFragment.kt:510`, `user/edit/place/UserEditActionZoneFragment.kt:109`), 2 redundant `else` branches (`discussions/DetailConversationActivity.kt:319`, `report/ActionSheetFragment.kt:639`), and rename the mismatched override parameter to `country` at `onboarding/onboard/OnboardingPhase1Fragment.kt:184`. Verify: 6 warnings gone.
- [x] 3.9 Run the measurement command and confirm the count dropped by 100 (356 → 256), then run unit tests. Verify: count is 256 and unit tests pass.

## 4. Tier 3 — Centralised compat helpers (207 warnings)

- [x] 4.1 Add `Activity.overrideTransitionCompat(@AnimRes enter: Int, @AnimRes exit: Int)` to `CompatExtensions.kt`, with the single `@Suppress("DEPRECATION")` and the comment required by design D2 (explaining that the API 34 `overrideActivityTransition` inverts the call site, so migrating is a separate animation refactor). Verify: helper compiles; exactly one suppression added.
- [x] 4.2 Replace all 56 `overridePendingTransition` call sites with `overrideTransitionCompat` across 17 files: `deeplinks/UniversalLinkManager.kt` (21 sites), `home/HomeFragment.kt` (8), `events/details/feed/EventFeedFragment.kt` (5), `groups/details/feed/GroupFeedFragment.kt` (5), `profile/MyProfileFullActivity.kt` (3), `MainActivity.kt` (2), `onboarding/login/LoginActivity.kt` (2), and one each in `actions/ActionsFragment.kt`, `enhanced_onboarding/EnhancedOnboarding.kt`, `events/EventsFragment.kt`, `groups/create/CreateGroupSuccessFragment.kt`, `groups/GroupsFragment.kt`, `guide/GuideHubFragment.kt`, `home/NationalGroupsActivity.kt`, `profile/ProfileFullActivity.kt`, `small_talks/SmallTalkActivity.kt`, `small_talks/SmallTalkIntroActivity.kt`. Verify: 56 warnings gone; screen-open and back animations unchanged when navigating group → feed → profile.
- [x] 4.3 Add reified `IntentCompat`/`BundleCompat`/`ParcelCompat` wrappers to `CompatExtensions.kt` (`Intent.serializableExtra<T>`, `Intent.parcelableExtra<T>`, `Bundle.serializableCompat<T>`, `Bundle.parcelableCompat<T>`, `Parcel.readParcelableCompat<T>`, `Parcel.readListCompat<T>`), deriving the class argument from the reified type so it cannot drift from the cast target. Verify: helpers compile with no suppressions.
- [x] 4.4 Migrate one call site per parcel/serializable variant first and run the affected screen, before bulk-applying — per design, a wrong class argument returns `null` silently rather than throwing. Verify: the piloted screen still receives its extra.
- [x] 4.5 Apply the parcel/serializable migration to the remaining 31 warnings across 25 files, including `groups/GroupModel.kt` (37,40,46,47,51 — this also clears the `incompatible upper bounds` warning at line 37, whose root cause is the untyped `readParcelable`) and `events/EventModel.kt` (53,58). Verify: 31 warnings gone; group and event objects still survive a Parcel round-trip (open detail screens and rotate).
- [x] 4.6 Replace 28 `bundleOf` call sites with `Bundle().apply { … }` across 21 files (per design D4 — no helper, since any wrapper reintroduces the type-unsafety the deprecation flags). Files include `MainActivity.kt` (614,627,632), `events/details/SettingsModalFragment.kt` (188,292,300), `notifications/NotificationActionManager.kt` (79,200,216), `groups/choosePhoto/ChooseGalleryPhotoModalFragment.kt` (119,146). Verify: 28 warnings gone; each affected fragment/bottom-sheet still receives its arguments.
- [x] 4.7 Replace 17 `getColor`/`getDrawable` sites with `ContextCompat.getColor` / `ContextCompat.getDrawable` across `discussions/DiscussionsListAdapter.kt` (171,172,176,177,193), `MainActivity.kt` (680,681,696,697), `notifications/InAppListNotificationsAdapter.kt` (84,85,88,89), `tools/view/WebViewFragment.kt` (370,371), `tools/Extensions.kt:93`, `home/RecommendationsListAdapter.kt:51`. Verify: 17 warnings gone; unread-conversation and notification rows keep their colours.
- [x] 4.8 Replace 7 `adapterPosition` with `bindingAdapterPosition` in `discussions/DiscussionsListAdapter.kt` (65,67,71,74), `home/NationalGroupsAdapter.kt` (70,80), `tools/view/countrycodepicker/CountryCodeAdapter.kt:27`. Verify: 7 warnings gone; tapping a row still opens the correct item after a list update.
- [x] 4.9 Clear the 68 long-tail deprecations across 39 files, fixing properly where a same-semantics replacement exists and using a single commented `@Suppress("DEPRECATION")` only where none does at `minSdk = 23`. Fix properly: `LocationRequest.create/setInterval/setFastestInterval/setPriority` → `LocationRequest.Builder` (`base/location/LocationUtils.kt:18-21`), `Locale(String)` → `Locale.forLanguageTag` and `updateConfiguration` → `createConfigurationContext` (`language/LanguageManager.kt:47,51,55`), `Locale.locale` → `resources.configuration.locales[0]` (6 sites), `setColorFilter` → `BlendModeColorFilterCompat` (4 sites), `ViewCompat.LAYOUT_DIRECTION_*`/`setLayoutDirection` → `View` constants (5 sites), `Html.toHtml`/`fromHtml` → `HtmlCompat` (3 sites), `systemUiVisibility` → `WindowInsetsControllerCompat` (`home/pedago/PedagoContentDetailsFragment.kt:112,126,136`), `IntentService` → `JobIntentService`/`WorkManager` or suppress (`widgets/WidgetMainUpdateService.kt:15`), `defaultDisplay`/`getMetrics`/`getRealMetrics` → `WindowMetricsCalculator` (3 sites), `Notification.defaults` → channel config (2 sites), `MediaStore.Images.Media.getBitmap` → `ImageDecoder` (`tools/utils/Utils.kt:332`), `onAttach(Activity)`/`onActivityCreated` → modern Fragment callbacks (2 sites), `RequestBody.create` argument order (`profile/association/AssociationPresenter.kt:140`). Justify each remaining suppression in a comment. Verify: 68 warnings gone; every suppression left in the tree is single-site and commented.
- [x] 4.10 Run the measurement command and confirm the count dropped by 207 (256 → 49), then run unit tests. Verify: count is 49 and unit tests pass.

## 5. Tier 4 — Activity Result API migration (49 warnings)

Highest-risk tier, sequenced last and split per feature area so each is independently revertible. Registration must happen before `STARTED`, so every launcher goes in a field initialiser, never inside a click listener. A half-migrated activity compiles fine and silently stops receiving results — so each task must remove the matching `onActivityResult` request-code branch in the same commit as it adds the launcher.

- [x] 5.1 Migrate the **actions** area (7 sites): `actions/ActionsFragment.kt` (343,349), `actions/detail/ActionDetailFragment.kt` (213,250,254,270), `actions/list/me/MyActionsListFragment.kt:136`. Verify: warnings gone; QA — create a contribution, create a solicitation, edit one, and confirm the list refreshes on return.
- [x] 5.2 Migrate the **events** area (14 sites): `events/EventsFragment.kt` (363,395,402), `events/details/SettingsModalFragment.kt` (171,179,195), `events/details/feed/AboutEventFragment.kt` (307,319), `events/details/feed/EventFeedFragment.kt` (415,427), `events/create/CreateEventSuccessFragment.kt:40`, `events/list/GroupEventsListFragment.kt:80`, `events/list/MyEventsListFragment.kt:81`. Verify: warnings gone; QA — create an event end-to-end, edit its settings, change its photo, and confirm the feed and list both refresh.
- [x] 5.3 Migrate the **groups** area (8 sites): `groups/details/feed/GroupFeedFragment.kt` (342,932,1115), `groups/details/GroupDetailsFragment.kt` (182,226), `groups/details/members/MembersFragment.kt:223`, `groups/GroupsFragment.kt` (500,506). Verify: warnings gone; QA — join a group, edit group settings, change the group photo, open a member profile.
- [x] 5.4 Migrate the **discussions, members and survey** areas (5 sites): `discussions/DetailConversationActivity.kt` (636,642), `members/MembersActivity.kt:368`, `survey/ResponseSurveyActivity.kt:75`, `user/edit/place/UserActionPlaceFragment.kt:230`. Verify: warnings gone; QA — send a photo in a conversation, open a member list, complete a survey, pick an action zone.
- [x] 5.5 Migrate the **home, profile and MainActivity** areas (6 sites): `home/HomeFragment.kt` (299,572,601,1138,1427), `MainActivity.kt:705`. Verify: warnings gone; QA — open national groups from home, open own profile, and confirm home refreshes on return.
- [x] 5.6 Migrate `deeplinks/UniversalLinkManager.kt` (4 sites: 189,207,275,367) — the hard case. It is not a `Fragment`/`ComponentActivity`, so the launcher must be owned by the hosting activity and passed in rather than registered locally. Verify: warnings gone; QA — open an `entourage-staging://` link and an `https://www.entourage.social/app/…` link for a group, an event and a user, from both cold and warm start.
- [x] 5.7 Remove the now-dead `onActivityResult` overrides and clear the 6 remaining `onActivityResult` / `overrides a deprecated member` warnings at `events/create/CreateEventStepOneFragment.kt` (63,64), `home/HomeFragment.kt` (303,304), `user/edit/place/UserActionPlaceFragment.kt:102`, `widgets/WidgetMainUpdateService.kt:17`. Verify: 6 warnings gone; no `onActivityResult` override remains that is only forwarding to a migrated launcher.
- [x] 5.8 Fix `MainActivity.kt:207` `startUpdateFlowForResult` — migrate to the `ActivityResultLauncher`-based in-app-update flow or suppress with justification. Verify: 1 warning gone; in-app update prompt still appears on a build with a lower `versionCode`.

## 6. Final verification

- [x] 6.1 Run the measurement command and confirm **0 warnings**. Verify: no `^w:` lines in the output.
- [x] 6.2 Run `./gradlew :app:testEntourageDebugUnitTest`. Verify: all unit tests pass.
- [x] 6.3 Run `./gradlew assembleEntourageRelease` — release differs from debug in `BuildConfig` IDs and R8 configuration, so a debug-only check is insufficient. Verify: release build succeeds with 0 warnings.
- [x] 6.4 Grep the diff for every `@Suppress("DEPRECATION")` added by this change and confirm each is single-site and carries a comment naming the reason and the API level that would allow its removal. Verify: list of remaining suppressions is enumerated in the PR description.
