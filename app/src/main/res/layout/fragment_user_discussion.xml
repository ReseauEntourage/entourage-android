<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/white"
                tools:context="social.entourage.android.user.discussion.UserDiscussionFragment">


    <RelativeLayout
        android:id="@+id/user_discussion_title_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:background="@color/feeditem_details_topbar_background"
        android:orientation="vertical">

        <ImageButton
            android:id="@+id/user_discussion_close"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:background="?android:attr/selectableItemBackground"
            android:contentDescription="@string/empty_description"
            android:padding="5dp"
            android:tint="@color/feeditem_details_topbar_tint"
            app:srcCompat="@drawable/ic_action_back"/>

        <ImageView
            android:id="@+id/user_discussion_icon"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@+id/user_discussion_close"
            android:layout_toRightOf="@+id/user_discussion_close"
            android:background="@drawable/bg_feeditem_icon"
            android:contentDescription="@string/empty_description"
            android:padding="3dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_user_photo"
            android:visibility="visible"/>

        <TextView
            android:id="@+id/user_discussion_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginEnd="10dp"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="5dp"
            android:layout_marginStart="5dp"
            android:layout_toEndOf="@+id/user_discussion_icon"
            android:layout_toLeftOf="@+id/user_discussion_info"
            android:layout_toRightOf="@+id/user_discussion_icon"
            android:layout_toStartOf="@+id/user_discussion_info"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:textColor="@color/feeditem_details_title"
            android:textSize="@dimen/entourage_font_large"
            android:textStyle="bold"
            tools:text="Test description"/>

        <ImageButton
            android:id="@+id/user_discussion_info"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="5dp"
            android:layout_marginRight="5dp"
            android:background="?android:attr/selectableItemBackground"
            android:contentDescription="@string/empty_description"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/ic_info_white_24dp"
            android:tint="@color/feeditem_details_topbar_tint"/>

    </RelativeLayout>

    <LinearLayout
        android:id="@+id/user_discussion_comment_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="false"
        android:layout_gravity="bottom"
        android:background="@color/background_light"
        android:orientation="horizontal"
        android:padding="5dp">

        <ImageButton
            android:id="@+id/user_discussion_comment_photo_button"
            android:layout_width="30dp"
            android:layout_height="24dp"
            android:layout_gravity="center_vertical"
            android:background="?android:attr/selectableItemBackground"
            android:contentDescription="@string/empty_description"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/ic_take_photo"
            android:visibility="visible"/>

        <EditText
            android:id="@+id/user_discussion_comment"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_weight="1"
            android:background="@drawable/bg_edit_text"
            android:hint="@string/empty_description"
            android:inputType="textMultiLine"
            android:maxLines="3"
            android:textSize="@dimen/entourage_font_large"/>

        <Button
            android:id="@+id/user_discussion_comment_send_button"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_gravity="center_vertical"
            android:background="?android:attr/selectableItemBackground"
            android:text="@string/send"
            android:textColor="@color/accent"
            android:textSize="@dimen/entourage_font_medium"
            android:visibility="visible"/>

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/user_discussion_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/user_discussion_comment_layout"
        android:layout_below="@+id/user_discussion_title_layout">

    </androidx.recyclerview.widget.RecyclerView>
</RelativeLayout>
