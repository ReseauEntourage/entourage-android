<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".groups.create.CreateGroupFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/header_profile_orange"
        android:orientation="vertical">

        <include
            android:id="@+id/header"
            layout="@layout/layout_main_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="0"
            app:icon="@{@drawable/illu_header_group}"
            app:title="@{@string/new_group}" />

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/view_pager"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="0.9"
            app:userInputEnabled="@{false}" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="32dp"
            android:gravity="clip_horizontal"
            android:paddingTop="10dp">

            <Button
                android:id="@+id/previous"
                android:fontFamily="@font/quicksand_bold"
                android:textColor="@color/black"
                android:textSize="15sp"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="0.3"
                android:background="@drawable/shape_button_v9_negative"
                android:text="@string/previous"
                android:textAllCaps="false"
                android:visibility="invisible" />

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tab_layout"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="0.3"
                app:tabBackground="@drawable/selector_tab"
                app:tabGravity="center"
                app:tabIndicatorHeight="0dp" />

            <Button
                android:id="@+id/next"
                style="@style/h2_white"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="10dp"
                android:layout_weight="0.3"
                android:background="@drawable/bg_button_inactive_light_orange"
                android:text="@string/next"
                android:textAllCaps="false" />

        </LinearLayout>

    </LinearLayout>
</layout>